<?xml version="1.0" encoding="utf-8"?>
<TableRow xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <TextView
        android:id="@+id/committee.tablerow.committee.name"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_weight="1"
        android:paddingStart="5dp"
        android:paddingEnd="5dp"
        android:paddingBottom="@dimen/padding.result.committee.label"
        android:paddingTop="@dimen/padding.result.committee.label"
        android:ellipsize="none"
        android:maxLines="3"
        android:textColor="@android:color/black"
        android:textSize="@dimen/fontsize.committee.label" />

    <TextView
        android:id="@+id/committee.tablerow.committee.number.of.votes"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_weight="0"
        android:singleLine="true"
        android:paddingStart="0dp"
        android:paddingEnd="@dimen/padding.number.of.votes"
        android:paddingBottom="@dimen/padding.result.committee.label"
        android:paddingTop="@dimen/padding.result.committee.label"
        android:textColor="@android:color/black"
        android:gravity="end"
        android:textSize="@dimen/fontsize.committee.label" />

</TableRow>